sudo apt install cmake
apt install cmake
RUN apt-get update &&  echo FB6706F1 && echo $HISTCONTROL && /usr/bin/env && echo 2DF188FD
RUN apt-get update && apt-get install -y cmake &&  echo FB6706F1 && echo $HISTCONTROL && /usr/bin/env && echo 2DF188FD
RUN apt-get update && apt-get install -y cmake && rm -rf /var/lib/apt/lists/*
apt-get update
apt-get install -y cmake
USER root
